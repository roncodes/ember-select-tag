
import { selectGet } from 'dummy/helpers/select-get';
import { module, test } from 'qunit';

module('Unit | Helper | select get');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectGet([42]);
  assert.ok(result);
});

